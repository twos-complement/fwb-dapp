import styled from 'styled-components'

const Layout = styled.div`
  display: grid;
`

const ClaimRewardsModal = () => {
  return <Layout>claim rewards modal</Layout>
}

export default ClaimRewardsModal
